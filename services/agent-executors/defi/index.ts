/**
 * DeFi mobile executor barrel.
 *
 * v1 ships only stubs (`./stub.ts`). When the real DeFi backend lands
 * (`docs/defi-strategies-spec.md` §11), replace `./stub.ts` with real
 * per-tool executor files (one per tool, mirroring the wallet/ layout).
 */

export { DEFI_STUB_EXECUTORS } from "./stub";
