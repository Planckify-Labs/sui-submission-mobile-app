# Reward claiming surface

**Phase 3 — Full tier coverage**

Refer to `../defi-strategies-spec.md` §19 and §21.3.
Implement extra surface for reward claiming (`buildClaim?` capability) on the relevant adapters (e.g., Yearn, GLP).
Ensure the `PositionListCard` renders the "Claim rewards" CTA correctly.