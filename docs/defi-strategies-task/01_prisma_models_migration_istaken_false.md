# Prisma models and migration

**Phase 1 — MVP**

Refer to `../defi-strategies-spec.md` §6 for the Prisma additions (`api/prisma/schema.prisma`):
- `UserStrategy`
- `StrategyPosition`
- `OpportunityCache`
- `ProtocolScoreCache`

Create the migration and ensure the new tables are created correctly.