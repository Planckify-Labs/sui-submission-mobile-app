# EVM Morpho Vaults adapter

**Phase 2 — Solana + cross-chain**

Refer to `../defi-strategies-spec.md` §19 (Phase 2).
Implement EVM Morpho Vaults adapter (Conservative — optimised lending) matching the `DefiProtocolAdapter` interface.