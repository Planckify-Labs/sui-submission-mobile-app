# LI.FI cross-chain routing proxy

**Phase 2 — Solana + cross-chain**

Refer to `../defi-strategies-spec.md` §10.
Implement LI.FI cross-chain routing via backend proxy (`api/src/strategies/external/lifi.client.ts`).
Add the `defi_cross_chain_deposit` mobile executor to handle the route normalisation and step execution.