# TakumiPay QR JWK Rotation Log

Every entry corresponds to one `EXPO_PUBLIC_TAKUMIPAY_QR_PUBKEY_JWK`
rotation executed per `docs/jwk_rotation_runbook.md`. Append-only —
never edit a historical row.

| `kid` (YYYY-MM-DD) | Operator | EAS Update ID | `takumipay-api` deploy SHA | Grace cut-off | Notes |
| --- | --- | --- | --- | --- | --- |
| _(no rotations yet — initial key still in use)_ |  |  |  |  | First rotation scheduled for production JWK cut-over per spec §10.1. |
